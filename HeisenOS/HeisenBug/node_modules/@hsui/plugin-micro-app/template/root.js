/**
 * root.js
 * 特殊地，约定以 root.js 作为框架与业务应用共享的状态模块，
 * 该模块下会以 root 模块的形式在业务应用挂载之后注册到各自的状态仓库中，而且与框架的 root 模块保持同步，
 * 注意，目前 root 模块的同步是单向的，即只支持由 frame 到 app 的状态同步
 */

export const state = () => ({
  theme: 'lightblue'
});

export const mutations = {
  switchTheme(state, theme) {
    state.theme = theme;
  }
};
